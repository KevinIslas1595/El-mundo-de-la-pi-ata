{
  // F5 arranca el servidor local y abre la pagina de inicio en Chrome.
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Abrir la pagina en Chrome",
      "url": "http://localhost:8080/principal.html",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Servidor local"
    }
  ]
}
